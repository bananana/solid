Hi!

"{{ post.author.initials }}" has posted a new discussion post on the  "{{ cause.title }}" page.

See it here:
	{{ url_for('posts.post_detail', slug=cause.slug, pk=post.id, _external=True) }}

Solidarity,

The {{ site_name }} team
