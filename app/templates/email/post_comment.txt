Hi!

There's a new reply to the discussion "{{ post.title }}" by  {{ post.author.initials }}:

{{ url_for('posts.post_detail', slug=post.cause.slug, pk=post.id, _external=True) }}

Solidarity,

The {{ site_name }} team
